name: build

on: [push, pull_request]

jobs:
    prepare:
        uses: userscripters/shared-actions/.github/workflows/workflow-nodejs.yml@main
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Compile
              run: npm run build-all
            - name: Run tests
              run: npm run test
